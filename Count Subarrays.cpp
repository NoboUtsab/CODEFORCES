#include<bits/stdc++.h>

using namespace std;

int main()
{
    int N,a[100000],i,count=0;
    cin>>N;
     for(i=0;i<N;i++)
    {
        cin>>a[i];
    }
    for(i=0;i<N;i++)
    {
        count++;
    }
    // while()
    cout<<count;
}
